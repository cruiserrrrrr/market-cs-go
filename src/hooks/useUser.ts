// import axios from "axios"
// import { useSelector } from "react-redux";
// import { BASE_URL_MOCK, USERLIST } from "../constanst/constants"
// import { useAuth } from "./useAuth";

// interface IUseUser {
//     result: Object
//     userBalance: number;

// }
// interface ITest {
//     telegramToken: any;
//     userBalance: number;
//     tgNoticeStatus: boolean;
//     email: string
// }
// // new Promise<(number)[](
// // export const useUser = new Promise<{ telegramToken: number; userBalance: number; tgNoticeStatus: boolean; }>( async () => {
// export function useUser() {
//     const { email } = useAuth();

//     let result: any;
//     axios.get(BASE_URL_MOCK + USERLIST)
//         .then(res => res.data)

//     const data = getData.data;

//     const userInfo: {
//         telegramToken: number;
//         userBalance: number;
//         tgNoticeStatus: boolean;
//         email: string
//     } = data.find(item => item.email === email)

//     // const user = data.find(item => item.email === email)
//     // console.log(userInfo)
//     // const telegramToken = userInfo.telegramToken;
//     // const userBalance = userInfo.userBalance;
//     // const tgNoticeStatus = userInfo.tgNoticeStatus;

//     const { telegramToken, userBalance, tgNoticeStatus } = userInfo;

//     console.log(telegramToken, userBalance, tgNoticeStatus)

//     return { telegramToken, userBalance, tgNoticeStatus }
// })

// import { createSlice } from "@reduxjs/toolkit";

// const initialState = {
//     telegramToken: null,
//     userBalance: null,
//     tgNoticeStatus: null,
// };

// const dataUserSlice = createSlice({

// })





































